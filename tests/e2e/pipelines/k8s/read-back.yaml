---
apiVersion: datuplet.io/v1
kind: Pipeline
metadata:
  name: "{{.RunPrefix}}-read-back"
  namespace: datuplet
spec:
  stages:
    - name: extract
      components:
        - name: json-extractor
          component: http-json-extractor
          version: dev
          config:
            url: "https://jsonplaceholder.typicode.com/posts"
          outputs:
            defaultBucket: "{{.RunPrefix}}-readback"
            defaultWriteMode: FULL_LOAD

    - name: read
      components:
        - name: stdout-writer
          component: stdout-writer
          version: dev
          inputs:
            tables:
              - bucket: "{{.RunPrefix}}-readback"
                table: data
          config:
            format: json
---
apiVersion: datuplet.io/v1
kind: PipelineRun
metadata:
  name: "{{.RunPrefix}}-read-back-run"
  namespace: datuplet
spec:
  pipelineRef:
    name: "{{.RunPrefix}}-read-back"
