# Known Limitations

Known limitations for the v0.1 experimental release. These are acknowledged
trade-offs, not oversights. Items marked "follow-up" are expected to improve in
later releases.

---

## Infrastructure

**CNPG backups not configured.** The Helm chart provisions CNPG without a backup
target. Data lives in the Postgres cluster (pipeline-api metadata, OpenFGA tuples,
Lakekeeper catalog) and in the object store (Iceberg parquet files). Enable WAL
archival via `cloudnative-pg.backup.enabled: true` + external S3 credentials before
any production use. The parquet files in S3/GCS are independently durable.

**Single-replica defaults.** Postgres (CNPG), Lakekeeper, OpenFGA, and
pipeline-observer all run at 1 replica. `pipeline-api` defaults to 2 replicas
(HTTP-only, stateless). HA configurations and leader-elected observer are follow-up
work.

**No OIDC-based database auth.** Postgres uses a preshared password + NetworkPolicy.
OIDC/OAuth DB auth drivers are immature for this stack today.

---

## Operations

**Secret rotation requires manual steps.** The signing key, Postgres passwords, and
OpenFGA API key are generated by the keygen Job on first install. Rotating them
requires `kubectl delete secret <name>` + `helm upgrade`. No rotation subcommand
exists today.

**Admin password rotation.** The admin credential Secret is created once by the
keygen Job. No rotation flow exists in v0.1.

**No alerting or observability templates.** No `ServiceMonitor` or `PrometheusRule`
templates ship with the charts. Recommended manual alert:
`pipelineapi_observer_lag_seconds > 300` for 5 minutes (observer is behind).

---

## Upgrade

**Breaking FGA model changes are manual.** When an OpenFGA DSL change renames or
removes a relation, existing tuples (project_admin, server-admin) need rewriting.
The only supported path today is: tear down the CNPG cluster (wipes OpenFGA +
Lakekeeper DBs), upgrade both charts, re-run `register.sh`. See
[docs/fga-model-upgrades.md](fga-model-upgrades.md).

**`platform.fgaModelVersion` cross-chart drift.** `charts/datuplet-app/values.yaml`
(`fgaModel.version`) and `charts/datuplet-lakekeeper/values.yaml`
(`platform.fgaModelVersion`) must be bumped together on every FGA model upgrade. CI
enforces DSL→version coupling within `datuplet-app` but does not enforce cross-chart
sync. Bump both values manually.

---

## Chart structure

**No umbrella chart.** The four-chart install is documented and tested; a single
umbrella chart wrapping all four phases is not provided for v0.1.

**Mutable `bitnami/kubectl:latest` tag.** The wait-for-platform init containers use
`bitnami/kubectl:latest`, which can change between upgrades. Pin to a versioned tag
for production installs.

**Reaper ServiceAccount is shared with pipeline-api.** The reaper CronJob uses
pipeline-api's ServiceAccount, which has broader verbs than the reaper needs. A
dedicated narrower ServiceAccount is follow-up work.

---

## Observer

**Status DB lag on observer failure.** The observer runs at exactly 1 replica.
If the observer Pod is down, the UI shows stale run-phase data until it recovers.
Actual pipeline execution continues; only status reporting is affected. Recommended
alert: `pipelineapi_observer_lag_seconds > 300` for 5 minutes.

**`runAsNonRoot` not set on observer.** The alpine base image runs as root by
default. Defense-in-depth via `capabilities.drop: [ALL]`, `allowPrivilegeEscalation:
false`, and `seccompProfile: RuntimeDefault` applies. A `USER` directive is
follow-up work.

---

## FGA model

**No automated FGA tuple migration tooling.** Breaking DSL changes (rename/remove
a relation) require a full cluster teardown and re-registration. Automated migration
tooling is follow-up work.

---

## Pipeline operator + TableCommit

**Cold-start tables fail.** Tables not yet registered in Lakekeeper fail with
"table not found in catalog" on first read. The Data Gateway creates tables on
first write via `LoadOrCreate`, so this path is rare in practice. Creating tables
at TableCommit time rather than at write time is follow-up work.

---

## JWT handling

**pipeline-api key rotation drops in-flight run tokens.** Run tokens carry a 24 h
`exp`. On a pipeline-api Deployment rollout, the signing key changes; any run token
signed by the previous key will fail validation at Data Gateway boot once the new
key takes effect. The `kid`-based JWKS client re-fetches once on a `kid` miss, but
key retention across restarts is not guaranteed in v0.1. Avoid rolling pipeline-api
during active runs.

**No NetworkPolicy restricting Data Gateway egress.** A compromised DG sidecar has
no cluster-level egress restriction. Adding a NetworkPolicy (allow only Lakekeeper +
pipeline-api + DNS) is recommended for production use.

---

## Validated deployment targets

GKE is the only validated cloud target for v0.1. EKS and AKS quickstarts are
expected in a later release.
