---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: componentdefinitions.datuplet.io
  labels:
    app.kubernetes.io/part-of: datuplet
    app.kubernetes.io/name: componentdefinition
spec:
  group: datuplet.io
  names:
    kind: ComponentDefinition
    listKind: ComponentDefinitionList
    plural: componentdefinitions
    singular: componentdefinition
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          ComponentDefinition is the Schema for the componentdefinitions API. It
          is cluster-scoped: the component registry is shared across all pipelines.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComponentDefinitionSpec defines a registered component
              and its available versions.
            properties:
              defaultVersion:
                description: |-
                  DefaultVersion is the version a pipeline resolves to when it omits
                  version. Empty resolves to the highest registered stable semver.
                maxLength: 64
                type: string
              deprecated:
                description: Deprecated marks the component as no longer recommended
                  for new pipelines.
                type: boolean
              description:
                description: Description explains what the component does.
                maxLength: 4096
                type: string
              displayName:
                description: DisplayName is the human-readable component name.
                maxLength: 256
                type: string
              io:
                description: |-
                  IO declares this component's input/output capability. Nil means
                  optional/optional (the component may or may not read/write tables).
                  IO lives at the definition level, not per-version, by design.
                properties:
                  inputs:
                    description: Inputs is the component's input capability mode.
                    enum:
                    - ""
                    - none
                    - optional
                    - required
                    type: string
                  outputs:
                    description: Outputs is the component's output capability mode.
                    enum:
                    - ""
                    - none
                    - optional
                    - required
                    type: string
                type: object
              maintainer:
                description: Maintainer identifies who owns this component definition.
                maxLength: 256
                type: string
              versions:
                description: Versions lists the available versions of this component.
                items:
                  description: VersionSpec defines a single version of a component.
                  properties:
                    configSchema:
                      description: ConfigSchema is a JSON Schema (draft 2020-12)
                        string blob validating component config.
                      maxLength: 262144
                      type: string
                    image:
                      description: Image is the container image for this version.
                      maxLength: 512
                      type: string
                    prerelease:
                      description: |-
                        Prerelease marks this version as excluded from default-version
                        resolution; prerelease versions may use mutable tags.
                      type: boolean
                    resources:
                      description: Resources bounds the compute resources this version
                        may use.
                      properties:
                        default:
                          description: Default is applied when the pipeline sets
                            no resources.
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This is an alpha field and requires enabling the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry
                                  in PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        max:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: Max is the resource ceiling; pipelines cannot
                            exceed it.
                          type: object
                      type: object
                    version:
                      description: |-
                        Version is the version identifier (e.g. "v0.1.0", or a mutable tag
                        such as "dev" when Prerelease is true).
                      maxLength: 64
                      type: string
                  required:
                  - image
                  - version
                  type: object
                  x-kubernetes-validations:
                  - rule: "self.prerelease == true || (self.image.contains(':') && !self.image.endsWith(':latest'))"
                    message: "stable versions must pin a non-latest image tag"
                maxItems: 50
                type: array
            required:
            - versions
            type: object
          status:
            description: ComponentDefinitionStatus defines the observed state of
              ComponentDefinition.
            properties:
              message:
                description: Message provides additional information about the
                  current phase.
                maxLength: 4096
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is "Valid" or "Invalid".
                maxLength: 32
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
