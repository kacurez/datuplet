apiVersion: v2
name: datuplet-operators
description: Datuplet operators — CNPG (CloudNativePG) operator. Install before datuplet-infra so the operator's mutating webhook has endpoints when the Cluster CR is applied.
type: application
version: 0.12.1
appVersion: "0.12.1"
home: https://github.com/kacurez/datuplet
sources:
  - https://github.com/kacurez/datuplet
maintainers:
  - name: kacurez
    email: tomas.kacur@keboola.com
dependencies:
  - name: cloudnative-pg
    version: "0.29.0"
    repository: https://cloudnative-pg.github.io/charts
    condition: cloudnative-pg.enabled
