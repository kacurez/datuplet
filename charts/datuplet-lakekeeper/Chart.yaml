apiVersion: v2
name: datuplet-lakekeeper
description: Datuplet Lakekeeper Iceberg REST catalog (Phase 4 of the 5-phase install). Requires datuplet-infra (Phase 2) + datuplet-app (Phase 3) installed first.
type: application
version: 0.1.0
appVersion: "1.0"
home: https://github.com/keboola/datuplet
maintainers:
  - name: kacurez
    email: tomas.kacur@keboola.com

dependencies:
  - name: lakekeeper
    version: "0.10.0"
    repository: https://lakekeeper.github.io/lakekeeper-charts
    condition: lakekeeper.enabled
    alias: lakekeeper
