/* PLACEHOLDER — not the real vendored library. See ui/appshell/vendor/VERSIONS
 * ("STATUS: NOT YET VENDORED") for why and what must replace this file
 * (pinned version 5.31.0) before charts can render.
 *
 * Fails loudly on use instead of silently pretending to be Vega. */
(function () {
  "use strict";
  function fail() {
    throw new Error(
      "ui/appshell/vendor/vega.min.js is a placeholder — see vendor/VERSIONS and vendor the real vega@5.31.0 build"
    );
  }
  if (!window.vega) {
    window.vega = { version: "PLACEHOLDER", expression: { functions: {} }, fail: fail };
  }
})();
