/* PLACEHOLDER — not the real vendored library. See ui/appshell/vendor/VERSIONS
 * ("STATUS: NOT YET VENDORED") for why and what must replace this file
 * (pinned version 6.29.0) before charts can render.
 *
 * Fails loudly on use instead of silently pretending to be vega-embed. Real
 * vega-embed exposes a callable default export attached here as
 * window.vegaEmbed (shell.js's mountVegaLiteChart calls window.vegaEmbed(el,
 * spec, opts)). */
(function () {
  "use strict";
  function vegaEmbed() {
    return Promise.reject(
      new Error(
        "ui/appshell/vendor/vega-embed.min.js is a placeholder — see vendor/VERSIONS and vendor the real vega-embed@6.29.0 build"
      )
    );
  }
  vegaEmbed.version = "PLACEHOLDER";
  if (!window.vegaEmbed) {
    window.vegaEmbed = vegaEmbed;
  }
})();
