/* PLACEHOLDER — not the real vendored library. See ui/appshell/vendor/VERSIONS
 * ("STATUS: NOT YET VENDORED") for why and what must replace this file
 * (pinned version 5.23.0) before charts can render.
 *
 * Fails loudly on use instead of silently pretending to be Vega-Lite. */
(function () {
  "use strict";
  function fail() {
    throw new Error(
      "ui/appshell/vendor/vega-lite.min.js is a placeholder — see vendor/VERSIONS and vendor the real vega-lite@5.23.0 build"
    );
  }
  if (!window.vegaLite) {
    window.vegaLite = { version: "PLACEHOLDER", compile: fail };
  }
})();
