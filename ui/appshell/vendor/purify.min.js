/* PLACEHOLDER — not the real vendored library. See ui/appshell/vendor/VERSIONS
 * ("STATUS: NOT YET VENDORED") for why and what must replace this file
 * (pinned version 3.2.3) before markdown blocks can render sanitized HTML.
 *
 * Fails loudly on use instead of silently pretending to sanitize. Real
 * DOMPurify exposes window.DOMPurify.sanitize(dirty, config). */
(function () {
  "use strict";
  function sanitize() {
    throw new Error(
      "ui/appshell/vendor/purify.min.js is a placeholder — see vendor/VERSIONS and vendor the real dompurify@3.2.3 build"
    );
  }
  if (!window.DOMPurify) {
    window.DOMPurify = { version: "PLACEHOLDER", sanitize: sanitize, isSupported: false };
  }
})();
