/* PLACEHOLDER — not the real vendored library. See ui/appshell/vendor/VERSIONS
 * ("STATUS: NOT YET VENDORED") for why and what must replace this file
 * (pinned version 14.1.4) before markdown blocks can render.
 *
 * Fails loudly on use instead of silently pretending to parse markdown. Real
 * marked exposes window.marked.parse(text) (and a callable window.marked(text)
 * on some majors). */
(function () {
  "use strict";
  function parse() {
    throw new Error(
      "ui/appshell/vendor/marked.min.js is a placeholder — see vendor/VERSIONS and vendor the real marked@14.1.4 build"
    );
  }
  if (!window.marked) {
    window.marked = parse;
    window.marked.version = "PLACEHOLDER";
    window.marked.parse = parse;
  }
})();
